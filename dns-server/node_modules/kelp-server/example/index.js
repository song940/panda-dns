const { createServer } = require('..');

class Home {
  constructor() {
    console.log('Home init');
  }
  index() {
    return "hello world";
  }
}

const server = createServer({
  routes: [
    `get / => Home#index`,
    `get / => Home#index`,
    `get / => Home#index`,
    `get / => Home#index`,
  ],
  middlewares: [],
  controllers: [Home],
  defaultErrorHandler: true,
});

server.listen(3000);
